//import StyleSheet from 'react-native';

// adjust as you need, just placeholders for now, no need to use all :)
export const constants = {
  // for spacing things out and changing later (padding, margin, gap, etc.)
  SPACER_1: 10,
  SPACER_2: 20,
  SPACER_3: 30,
  SPACER_4: 40,
  SPACER_5: 50,
  BORDER_WIDTH: 2,
  // colors
  COLOR_WHITE: '#fafafa',
  COLOR_BLACK: '#333333',
  COLOR_BORDER: '#bababa',
  COLOR_ACTION: '#10de81', // eg. primary button background color, maybe useless because of Native Paper
};

/* 
//This doesn't seem to work, we could just have the styles and create the StyleSheet object in the Screens
// commonly used component styles (eg. container)
export const commonStyles = StyleSheet.create({
  widgetsContainer: {
    display: 'flex',
    flexDirection: 'column',
    paddingTop: constants.SPACER_1,
    paddingBottom: constants.SPACER_1,
  },
});
*/
